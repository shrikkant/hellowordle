services:
  jenkins:
    build: .
    container_name: wordbaazi-jenkins
    ports:
      - "${JENKINS_PORT:-8090}:8080"
    volumes:
      - jenkins-home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

volumes:
  jenkins-home:
