# Google OAuth Web client ID (baked into the client bundle at build time)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# Google Tag Manager container ID (leave empty to disable analytics in dev)
NEXT_PUBLIC_GTM_ID=
